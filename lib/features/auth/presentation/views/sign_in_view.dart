import 'package:flutter/material.dart';

class SignInView extends StatelessWidget {
  const SignInView({super.key});
  static String path = '/SinInView';

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
